# Dependencies are declared in pyproject.toml, which is the single source of
# truth. This file exists so `pip install -r requirements.txt` still works.
-e .

# Barcode decoding is an optional extra because it needs a system library:
#   brew install zbar   /   apt install libzbar0
# then:  pip install -e '.[barcodes]'
